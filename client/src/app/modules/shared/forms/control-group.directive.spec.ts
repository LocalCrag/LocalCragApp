import { ControlGroupDirective } from './control-group.directive';

describe('ControlGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlGroupDirective();
    expect(directive).toBeTruthy();
  });
});
