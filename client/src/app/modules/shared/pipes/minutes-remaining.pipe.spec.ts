import {MinutesRemainingPipe} from './minutes-remaining.pipe';

describe('MinutesRemainingPipe', () => {
  it('create an instance', () => {
    const pipe = new MinutesRemainingPipe();
    expect(pipe).toBeTruthy();
  });
});
