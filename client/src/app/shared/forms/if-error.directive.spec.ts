import { IfErrorDirective } from './if-error.directive';

describe('IfErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new IfErrorDirective();
    expect(directive).toBeTruthy();
  });
});
