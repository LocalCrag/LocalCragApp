import { marker } from '@jsverse/transloco-keys-manager/marker';

marker('BOULDER');
marker('SPORT');
marker('TRAD');

export enum LineType {
  BOULDER = 'BOULDER',
  SPORT = 'SPORT',
  TRAD = 'TRAD',
}
