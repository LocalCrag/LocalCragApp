{
  "name": "localcrag-server",
  "version": "1.23.1",
  "devDependencies": {
    "lint-staged": "^15.4.3"
  },
  "lint-staged": {
    "**/*.py": [
      "pipenv run black .",
      "pipenv run flake8 .",
      "pipenv run isort ."
    ]
  }
}
